#### **JAVASCRIPT**



##### Day- 1

###### Introduction JavaScript

\- The first was launched in the year of 1995 create in the just 10 days.

\- The JavaScript Latest version is ECAM2026 in the year of 2026.

\- The JavaScript is dynamic programming which used to build a logic in the webpages and the only one programming language that can interact with the webpage easily.

\- In JavaScript the previous can't removed and the next version it will continue the previous versions.

\- The biggest updated was done in the year of 2015.

\- The JavaScript first named mocha then was changed as Live Script then it was changed as JavaScript because in those days the popular language is Java. so to attract java developer it was named as JavaScript.

\- The script means giving some instruction to the code to do something in the code. Through JavaScript we can develop the backend and frontend.

\- To write the JavaScript there are 4 ways

\- 1. we can open any browser in that go to console we can write the code in the console box but is an temporary storage

\- 2. In Second way we can create file with extension of .js so we can run code in the VScode of terminal

\- 3. In the third way we can create the script file and also we can create the index file we can link the both file using script tag we can the file.

\- 4. The fourth way is using the alert box giving the alert message like pop.

\- The JavaScript is the strictly case-sensitive.

* The JavaScript is Interpreted base language.
* To access the element in the JavaScript we can **document.getElementbyId()** is use to access the particular element in the html tag

###### Variables



\- There are main there types of variable are used in the JavaScript there are let, const and var

\- The Variable can hold any type of data

\- The \*\*var\*\* is the old version variable and it is global variable means we can access the values any where in the file and also we can create number duplicate variable name using var keyword

\- The second one is \*\*Let\*\* keyword it is block scope with in the block it can access the element we can change value but we can't create duplicate variable names in the same block of code.

\- The let is alterative of var keyword but with updated version.

\- The Third one is const means we change the value in the value and it also a block scope keyword. The const is can't Reassigned the value because it fix with one variable only.

